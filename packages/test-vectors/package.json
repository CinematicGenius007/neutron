{
  "name": "@neutron/test-vectors",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate:envelopes": "tsc --build && node scripts/write-envelope-candidates.mjs",
    "test:browser": "vitest run --config test/vitest.browser.config.ts",
    "test:generate": "vitest run --config test/vitest.generator.config.ts"
  },
  "dependencies": {
    "@neutron/protocol": "workspace:*"
  },
  "devDependencies": {
    "@noble/ciphers": "2.2.0",
    "@noble/hashes": "2.2.0",
    "@vitest/browser-playwright": "4.1.10",
    "ajv": "8.17.1",
    "playwright": "1.62.0"
  }
}
